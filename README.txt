make a each loop out of the function where every time that the object is equal to the argument it places a specific += counter in that slot for that hash. The logical next step would be to recognize when an argument is equal to an element in the array

  separate.include?(find)
